# 	$id
# 	$repository
# 	$method
# 	$release_name
#	$release_path
#	$shared_path
#	${!branch}
#   ${!deploy_to}
#	${hosts[@]}
#	${tasks[@]}
#

track "screen" "$green$counter) Project $id$std"

for hostid in "${hosts[@]}"
do
	host="hosts_"$hostid"_host"
	user="hosts_"$hostid"_user"
    last_ssh_target="AAA"${!user}@${!host}
	track "screen" "Working on host $green"${!host}"$std"
	exec_wrap "$id:test:listremotefiles" ssh ${!user}@${!host} ls ${!deploy_to}
done

track "screen" $green"Ok things seem to be all right"$std